#include<stdio.h>

int main()
{
float d,r;
printf("Enter a degree measure you want to convert into radian");
scanf("%f",&d);
r=d/57.295779;
printf("The radian measure for the entered angle is %f",r);
return(0);
}
